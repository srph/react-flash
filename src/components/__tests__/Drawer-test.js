var src = '../Drawer.jsx';
jest.dontMock(src);

describe('Drawer component', function() {
  it('should contain messages with `flash` as type');
  it('should contain messages with `snack` as type');
  it('should contain messages with `toast` as type');
});